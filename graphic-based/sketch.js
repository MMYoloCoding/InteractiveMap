
let drawButton;
function setup() {
  createCanvas(500, 500);
  background(220);   // Set the background color to gray
  drawButton = createButton("Draw Zone");
}

// function draw() {
//   if (mouseIsPressed) {
//     fill(255);
//     ellipse(mouseX, mouseY, 8, 8);
//   } else {
//     fill(255);
//   }
  
// }